    <div class="w-full flex gap-2 flex-start items-center">
            <input id="{{ $id }}" name="{{ $id }}" type="checkbox" checked
                class=" focus:ring-primary h-4 w-4 text-primary border-gray-300 rounded accent-violet-700" value="{{$legend}}">
            <label for="{{ $id }}" class="text-sm font-regular text-gray-700">{{ $legend }}</label>
    </div>
